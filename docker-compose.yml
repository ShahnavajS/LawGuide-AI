services:
  lexiguide:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lexiguide-ai
    restart: unless-stopped
    ports:
      - "3000:8080"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=/app/data/lexiguide.db
      - STORAGE_DIR=/app/uploads
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-2.5-flash}
      - APP_SESSION_SECRET=${APP_SESSION_SECRET:?Set APP_SESSION_SECRET in .env}
      - EVALUATOR_DEMO_ENABLED=${EVALUATOR_DEMO_ENABLED:-false}
      - EVALUATOR_DEMO_NAME=${EVALUATOR_DEMO_NAME:-Evaluator Demo}
      - EVALUATOR_DEMO_EMAIL=${EVALUATOR_DEMO_EMAIL:-evaluator@lexiguide.demo}
      - EVALUATOR_DEMO_PASSWORD=${EVALUATOR_DEMO_PASSWORD:-}
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
